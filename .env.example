# Get your free API key from: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# CRITICAL: Generate a strong random secret for production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=CHANGE_ME_TO_RANDOM_32_BYTE_STRING

# Used to encrypt Binance API credentials in the database
# Leave empty to automatically generate a secure key on first run
BINANCE_CRED_ENC_KEY=
